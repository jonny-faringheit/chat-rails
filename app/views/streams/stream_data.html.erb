<script>
  const sse = new EventSource('/streaming');
  sse.onmessage = (event) => {
    console.log('New user added:', event.data);
  }
  sse.onerror = (error) => {
    console.error('Error in SSE:', error);
  };
  sse.onopen = () => {
    console.log('SSE connection opened');
  }
</script>